# === App ===
ENVIRONMENT=development
LOG_LEVEL=info

# === CORS/Hosts (ajusta con tu dominio de Codespace) ===
# Puedes dejar "*" en desarrollo, pero es mejor ser explícito
ALLOWED_ORIGINS=https://fluffy-goggles-jvw66grg9gv3q4pp-8000.app.github.dev,http://localhost:3000
ALLOWED_HOSTS=*

# === Auth/JWT ===
# Genera una clave segura: `openssl rand -hex 32`
SECRET_KEY=REEMPLAZA_POR_UNA_CLAVE_HEX_SEGURA
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# === Base de datos ===
# Opción A: URL directa (recomendada para backend)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/railway

# Opción B: variables sueltas (si tu compose las usa para el contenedor de postgres)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=railway
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Pooling (opcional)
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# === Frontend (para evitar warnings aunque no uses el servicio ahora) ===
NEXT_PUBLIC_API_BASE=https://fluffy-goggles-jvw66grg9gv3q4pp-8000.app.github.dev
NEXT_PUBLIC_APP_NAME=Evaluacion Nutricional
NEXT_PUBLIC_VERSION=0.1.0
NODE_ENV=development
